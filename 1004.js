export function problem(lines) {

    // Atribuição por desestruturação:
    const[a,b] = lines;
    const prod = a * b;
    console.log(`PROD = ${prod}`);









    // Minha solução: 
    // Não funciona no Beecrowd.
    // const prod = (a, b) => a * b;
    // const resultado = lines.map(Number).reduce(prod);
    // console.log(`PROD = ${resultado}`);

}
